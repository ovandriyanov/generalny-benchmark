# GOOGLE_BENCHMARK_INCLUDE_PATH="${GOOGLE_BENCHMARK_INCLUDE_PATH}"
# GOOGLE_BENCHMARK_LIB_PATH="${GOOGLE_BENCHMARK_LIB_PATH}"

benchmark: benchmark.cpp
	g++ benchmark.cpp -o benchmark -std=c++14 -lbenchmark -pthread -I "${GOOGLE_BENCHMARK_INCLUDE_PATH}" -L "${GOOGLE_BENCHMARK_LIB_PATH}"
